// Generated by `just generate-models`. Do not edit.
// Declared in core/bin/generate/remote_types.yml.

import BigInt
import Foundation
import Primitives

public extension Account {
    static func mock(
        chain: Chain = .bitcoin,
        address: String = "",
        derivationPath: String = "",
        extendedPublicKey: String? = nil,
    ) -> Account {
        Account(
            chain: chain,
            address: address,
            derivationPath: derivationPath,
            extendedPublicKey: extendedPublicKey,
        )
    }
}

public extension NFTImages {
    static func mock(
        previewImageUrl: String = "",
    ) -> NFTImages {
        NFTImages(
            previewImageUrl: previewImageUrl,
        )
    }
}

public extension PriceAlert {
    static func mock(
        price: Double? = nil,
        type: String = "",
        lastNotifiedAt: Date? = nil,
    ) -> PriceAlert {
        PriceAlert(
            price: price,
            type: type,
            lastNotifiedAt: lastNotifiedAt,
        )
    }
}

public extension PriceAlertNotification {
    static func mock(
        walletId: WalletId = .mock(),
        alert: PriceAlert = .mock(),
        chain: Chain = .bitcoin,
        state: ConnectionState = .notReachable,
        count: Int32 = 0,
        amount: Double = 0,
        votes: UInt32 = 0,
        value: String = "0",
        read: Bool = false,
        createdAt: Date = Date(timeIntervalSince1970: 0),
        accounts: [Account] = [],
        note: NFTImages? = nil,
        path: String = "m/44'/0'/0'/0/0",
    ) -> PriceAlertNotification {
        PriceAlertNotification(
            walletId: walletId,
            alert: alert,
            chain: chain,
            state: state,
            count: count,
            amount: amount,
            votes: votes,
            value: value,
            read: read,
            createdAt: createdAt,
            accounts: accounts,
            note: note,
            path: path,
        )
    }
}
