// Generated by `just generate-models`. Do not edit.
// Declared in core/bin/generate/remote_types.yml.

import Foundation
import Primitives

extension Primitives.Account {
    func toRecord(walletId: String) -> AccountRecord {
        AccountRecord(
            walletId: walletId,
            chain: chain,
            address: address,
            extendedPublicKey: extendedPublicKey,
            derivationPath: derivationPath,
        )
    }
}

extension AccountRecord {
    func toAccount() -> Primitives.Account {
        Primitives.Account(
            chain: chain,
            address: address,
            derivationPath: derivationPath,
            extendedPublicKey: extendedPublicKey,
        )
    }
}

extension Primitives.Position {
    func toRecord(walletId: String) -> PositionRecord {
        PositionRecord(
            id: id,
            walletId: walletId,
            leverage: Int(leverage),
            entryPrice: entryPrice,
            takeProfit: takeProfit,
            previewImageUrl: images.previewImageUrl,
            tags: tags,
            state: state,
            updatedAt: updatedAt,
        )
    }
}

extension PositionRecord {
    func toPosition() -> Primitives.Position {
        Primitives.Position(
            id: id,
            leverage: UInt8(leverage),
            entryPrice: entryPrice,
            takeProfit: takeProfit,
            images: Primitives.NFTImages(previewImageUrl: previewImageUrl),
            tags: tags ?? [],
            state: state ?? .connected,
            updatedAt: updatedAt,
        )
    }
}
