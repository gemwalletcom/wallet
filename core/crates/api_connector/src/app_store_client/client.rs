pub struct AppStoreClient {}

impl Default for AppStoreClient {
    fn default() -> Self {
        Self::new()
    }
}

impl AppStoreClient {
    pub fn new() -> Self {
        AppStoreClient {}
    }
}
