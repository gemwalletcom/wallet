# Open work

The goal is that Gemstone decides once and both clients read that decision. Shared rules and orchestration live in Core; rendering, observation, scheduling and locale formatting live in the apps. Contracts and implementation examples are in [ARCHITECTURE.md](ARCHITECTURE.md).

Every open item carries a stable id (U duplicated or redundant code, V vocabulary and twins, R rows, C composition, S sessions, B view boundary, F formatting and errors, P parity, D decisions, O ownership, K Core shapes, X platform, G guidance, PERF performance, MIG migration foundations, AUD follow-up review) and a size (**S**/**M**/**L**). Ids are never reused. **Delete an item's line in the commit that lands it**; an item that turns out to be correct as written is closed the same way, with the reason moved to the ledger at the bottom.

**Migration plan reviewed on 2026-09-19 at `f39c2c61a59841fa2307d78e05a231052162418f`.** Preserve the existing backlog and its stable ids; this pass rechecked 56 existing items across Core and both apps, corrected unsafe prescriptions and added six foundation tasks. The earlier six-area review remains the source for the other retained items. This is a plan, not a claim that the proposed interfaces or automated checks already exist. Do not add duplicate tasks to reach a count.

## Working one item

Pick one item by its stable id and use the screen map to find its current owner. Source paths and line numbers are retrieval hints, not proof that an old finding still applies. Read the owner, both consumers and the linked architecture example before editing; closed ledger entries are dated rationale, not current completion claims.

Keep the implementation plan in the task update: the existing owner to change, the app path to delete, any real prerequisite and the exact relevant checks. Do not create a new planning artifact, service, session, cache or framework unless the current contract requires it. If a pure projection or existing record answers the question, use it. Preserve examples in the docs and correct them in place when signatures change. Check related ids before changing a shared owner: reuse one contract change and its regression fixtures rather than implementing competing fixes. AUD13/AUD14 share the currency contract; MIG6/AUD35 use the same persistence tests, with coverage and CI execution remaining distinct requirements.

Use [Task Workflow](../skills/task-workflow.md) for execution and [Quality Checks](../skills/quality-checks.md) for verification. Implement ready work autonomously; the items in [Decisions to make](#7-decisions-to-make) require a product/security choice, and [Blocked upstream](#8-blocked-upstream) records external prerequisites. A `D` prefix alone does not mean an item is blocked. Shared infrastructure work gates only the consumers that need it; unrelated ready items need not wait for an entire phase.

## Execution order

1. **Protect correctness:** resolve D72/D73 before changing their security behavior. Preserve existing auth and transaction integrity contracts.
2. **Establish consistency:** MIG6. Use MIG5 to prevent new boundary regressions while the remaining debt is reduced.
3. **Move complete workflows:** U19 payments, C52 deep-link/push preparation, C53 wallet creation/import with D43/R124, and the device observers. Keep native routes and lifecycle executors.
4. **Migrate screen families:** follow the coverage map below. Within each family settle state and actions before rows, then remove app branches, duplicate models, formatters and exports in the same change. Dependencies are not permission to bundle unrelated families.
5. **Close the boundary:** finish B76–B80, U9, U18, U33/U35, U36, F56/F61/F62 and O59/O60 where their owners are ready. Re-run the coverage audit; a matching service field alone is not completion.

## Screen coverage and existing infrastructure

This map routes work to current owners. It groups existing ids rather than creating a new service or task for every view. Shared components and platform-only features keep their established boundaries; the service map in [ARCHITECTURE.md](ARCHITECTURE.md#screen-services) supplies the current app callers.

| Screens / entry points | Existing owner or infrastructure to extend | Open work |
|---|---|---|
| Create/import wallet, terms, phrase generation | `GemWalletService`, import records, keystore and native auth ports | C53, D43, R124, X172 |
| Wallet list/detail, rename, avatar, secret export | Wallet rows/details, existing export flow and NFT avatar selection | B79, R107, K17, X172 |
| Wallet home, header, network assets, banners | `GemWalletHomeService`, `GemBalanceService`, shared asset rows and banner context | R95, R111–R113, R133, U25, D46, D56, O59 |
| Asset search/select, add token, recents | `GemAssetSelectionService`, `GemSelectAssetFlow`, `GemAddAssetService`, recent activity | R111/R133, D56, B76/B80, K18 |
| Asset details and asset actions | `GemAssetDetailsService`, shared rows, copy, info and load state | R102, R132, B76/B80, U30 |
| Portfolio chart/statistics | `GemPortfolioService`, chart load rules, shared numbers and rows | S74, R94, D70, O59, F61/F62 |
| Asset chart/market/alerts sections | `GemChartService`, `GemChartSession`, shared list renderer | S72, R128, D70, B76, F61/F62 |
| Receive, QR display, address details | `GemReceiveService`, `GemAddressDetailsService`, `GemCopy`, payment encoding | R132; retain existing native QR/share adapters |
| Scanner, payment links, deep links and pushes | Existing payment decoder, `GemPaymentService`, push/navigation preparation | U19, C52, D49, F56/F60 |
| Recipient/address/name input | `GemRecipientSession`, `GemNameService`, existing input component | V92; keep debounce/observation native |
| Amount entry, fiat equivalent, amount extras | `GemAmountService`, `GemAmountEntry`, existing provider inputs | B78, R120, D55, V91 |
| Confirmation, fees, simulation, acquisition | `GemConfirmTransferService`, `GemConfirmation`, shared headers/rows/info | C51, R89/R90/R93, U30, D55, F58, P90, D73 |
| Swap, providers, slippage and swap details | `GemSwapQuoteService`, `GemSwapSession`, `GemSlippageSession` | S82, D67, R91/R129, U15, D55 |
| Activity, asset/position history, transaction details | `GemTransactionsService`, detail records, native indexed queries | U18, R121/R127, P90, F62 |
| Buy/sell quotes, provider opening, fiat history | `GemFiatQuoteService`, `GemFiatSession`, existing fiat transaction owner | S83, R120/R122, U15, S80 |
| Perpetual market list/search/pins and balance | `GemPerpetualService`, market session/rows, native search indexes | D66, R98/R112, K14/K19, O59 |
| Perpetual position/details/candles/activity | `GemPerpetualDetailsService`, position rows, chart load rules | S73/S81, R96/R128, F61/F62 |
| Perpetual open/modify/autoclose forms | Existing amount flow and `GemAutocloseSession` | S75, V91, K14 |
| Stake, validators, delegation and claim | `GemStakeService`, validator/delegation records, generated transfer input | R100/R101/R118; preserve exact atomic values |
| Earn list, provider and deposit amount | Existing stake/earn owner and amount extras | R100/R101; preserve the existing feature gate |
| NFT root/collection/unverified, detail/report/avatar | `GemNftService`, `GemCollectibleService`, shared rich rows and avatar flow | R114, S80 |
| Price-alert list/target/auto-alert controls | `GemPriceAlertService`, alert session and existing notification port | R129, D68/D69, P97, S80, B80 |
| Rewards/create/use/redeem referral | `GemRewardsService`, rewards state, shared load and list records | S79, R104/R123, B77, V91 |
| Contacts/list/editor/address picker | `GemContactService`, `GemManageContactService`, contact session/name component | U21, O60, V92 |
| Networks/node list/add/check | `GemChainSettingsService`, node sessions, shared rows | K20, R131 |
| Settings/preferences/currency/language/appearance | `GemSettingsService`, `GemCurrencyService`, preference observation | B77, O59; retain native locale/theme application |
| Security/lock/biometry/recovery | `GemSecurityService`, existing keystore/auth ports and settings sections | D72, B77, X172; retain platform-only privacy lock |
| Push settings, in-app notifications, support chat | Notification services, `GemSupportService`, permission and lifecycle ports | P97, D48/D49/D58, S80, B77/B80 |
| WalletConnect list/detail/proposal/request/signing | `GemWalletConnectService`, `GemSignMessageService`, Reown adapters | F57, D72/D73; retain Android-only one-click auth |
| About, app update, developer/service status | Existing settings/update/developer services and native store adapters | R131, B77; platform delivery channels remain distinct |
| Widgets and shared display components | `GemWidgetService`, `GemFormattedNumber`, shared rich/plain renderers | U9/U17, F61/F62; retain native widget scheduling |

## Completion contract for every item

- Name the existing Core owner, native consumers and infrastructure being reused. A new type/service/session needs an actual missing responsibility; a pure projection or one-input screen is not a reason for a wrapper.
- Implement domain behavior and meaningful tests in Core; preserve exact values, wallet/request identity, atomic writes, failure outcomes, cancellation and auth. Native SQL may retain indexed filtering/sorting while implementing the same product query contract.
- Wire both apps, remove the replaced path, and keep platform mapping/observation thin. Use generated models, shared rows/copy/info/number/load infrastructure and module localization/style mappers. Reuse native routes, OS ports, Room/GRDB transactions and testkits.
- Test the contract being moved: Core decisions/ordering; real adapters for transaction, mapping and query behavior; app tests for wiring and visible output. For async screens include stale success/failure, wallet/asset/currency changes, empty first load and refresh-with-existing-content as applicable. Do not remove meaningful integration coverage under U33.
- Run the applicable [Quality Checks](../skills/quality-checks.md), regenerate/build both apps when the boundary changes, and exercise reachable changed UI. Delete the item's line when complete and remove its references from this map/order or replace them with the completed contract. Do not claim that a static scan proves product parity.

## Migration foundations

These are additional tasks from the architecture review. MIG ids are a new namespace; the implementations described below are proposed.

- **MIG5** **M** Most documented boundary censuses are not checked-in gates: Repo Checks runs mapper parity and doc links, the mapper checker only compares variants found on both sides, and no other documented rule has a check. Add small reproducible checks through the existing script/`just` infrastructure, starting with one reliably detectable regression such as misplaced localization mapping or service construction. Expand to feature-view Core calls and direct writes only where the check can distinguish the allowed paths without a new analysis framework. For reliable checks, record current debt by path/rule with its TODO id, reject new violations and stale exceptions, and run them in the existing appropriate checks/CI. Keep ambiguous semantic findings advisory. Exempt generated code, native ports/DAOs, DI providers, shared row renderers and legitimate child dependencies explicitly. Treat ambiguous matches as review leads; do not generate wrapper classes just to satisfy a regex.
- **MIG6** **M** Add paired persistence-contract cases using the existing GRDB/Room test infrastructure and Core store testkits for the operations changed by the balance publication lane: rollback, conditional conflict handling, missing rows, wallet isolation, unchanged writes and observer-visible batches. Use equivalent fixtures and expected results on both platforms; test the real adapters and transaction runner. Keep sequencing/failure/retry assertions in Core and mapping/query assertions native. Land the relevant tests with each behavior change, not as a later verification phase. This closes the verification gap without introducing a generic persistence framework or duplicating domain-rule tests.

## Follow-up correctness and consolidation review

Reviewed at `855fef5ccfba98458b9bfaa0efdea01ef23c0a07` on 2026-09-19. These are additional source-traced findings, checked against the open items and closed ledger. Failure interleavings below still need the named regression tests; this review did not reproduce every issue in a running app. Fix the existing owner and reuse native persistence, lifecycle, error and test infrastructure. Keep the wallet USD price contract: fetch USD prices, retain the USD value, and apply fiat rates once in Gemstone; the independent widget pricing path is intentional.

### Core and shared data correctness

- **AUD5** **M** **Bug — a sent transaction that fails to record leaves no local row and no tracking.** [`store_pending`](../core/gemstone/src/services/confirm/mod.rs) calls `record(...).unwrap_or_default()`, so a failed `add_transactions` write becomes an empty list: no pending row is stored, `track` is handed nothing, and `execute` returns `Sent` as if everything succeeded. The funds are on chain and the activity sync will eventually fetch the transaction, so what is lost is local visibility and state polling, not funds. Do not route the outcome through the confirmation result: both apps read only `hashes` from `GemExecuteResult::Sent` ([iOS](../ios/Features/Transfer/Sources/ViewModels/ConfirmTransferSceneViewModel.swift), [Android](../android/features/confirm/viewmodels/src/main/kotlin/com/gemwallet/android/features/confirm/viewmodels/ConfirmViewModel.kt)), and its `transactions` payload was removed as dead FFI weight, so a new field or enum there would be read by nobody. This needs the product answer first — a warning on the success screen, a silent retry of the write, or documented reliance on the next activity sync — then one implementation at the owner. Cover the `Broadcast` error path too, where `store_pending` already runs for a partial broadcast.
- **AUD13** **M** **Bug — prevent old-currency writes after a currency switch.** [Asset loading](../core/gemstone/src/services/assets/mod.rs) captures currency before network I/O; [stream handling](../core/gemstone/src/services/stream/mod.rs) and [currency switching](../core/gemstone/src/services/currency/mod.rs) also perform independently awaited price writes. A delayed old-currency update can overwrite newly converted prices. Coordinate publication in the existing price/currency owner, applying USD data with the current rate at commit. Test a suspended refresh and rapid currency changes; native transactions alone do not establish ordering.
- **AUD14** **M** **Bug — keep cached market figures tied to their currency.** [Market conversion](../core/gemstone/src/services/price/rules.rs) stores already-converted market cap, volume and ATH/ATL, but currency changes only reprice spot prices. [Chart sections](../core/gemstone/src/services/chart/mod.rs) label cached market data using the new preference, including before refresh or after offline failure. Keep USD market values and convert once when projecting, reusing the price owner. Test currency/rate changes with cached data and no network response; preserve supply and percentage fields.

### iOS lifecycle and result handling

- **AUD15** **S** **Bug — cancel pending perpetual connection preparation.** [Hyperliquid observer](../ios/Packages/GemstoneServices/Sources/Perpetual/HyperliquidObserverService.swift) awaits connection preparation before installing its task; disconnect sees no task and returns. An obsolete preparation can subsequently connect in the background or replace a newer wallet. Invalidate preparation on disconnect/setup and check its identity before installation. Initial lifecycle setup and wallet-change setup can also enter preparation concurrently for the same wallet before `currentWallet` is assigned. Coalesce that pending same-wallet work in this owner. Test one preparation/connect/retained observer for overlapping identical setup, plus delayed wallet A → wallet B and delayed preparation → background.
- **AUD17** **S** **Bug — report WalletConnect failures before the socket opens.** [WalletConnect WebSocket](../ios/Packages/FeatureServices/WalletConnectorService/WebSocket.swift) only calls `onDisconnect` when `isConnected` is true. Initial DNS/TLS/handshake errors therefore never reset Reown's connecting state or trigger its retry. Deduplicate completion per attempt, including pre-open failure. Verify one disconnect callback for a failed handshake, retry eligibility and duplicate close/completion callbacks.
- **AUD20** **S** **Bug — make explicit lookup retries run.** [Add-token triggers](../ios/Features/Assets/Sources/ViewModels/AddAssetSceneViewModel.swift) clear the session before assigning an equal immediate trigger; [debounced tasks](../ios/Packages/Components/Sources/ViewModifiers/DebouncedTaskModifier.swift) only restart on change. Repeating paste/submit can clear a result or leave a failed lookup unretried. Add-node paste/scan has the same pattern. Distinguish explicit retry from changed input in the existing action. The [add-node Done action](../ios/Features/Settings/Sources/ChainSettings/Scenes/AddNodeScene.swift) also calls `load()` directly while its debounce remains scheduled, checking the same URL twice. Route submit through the same owned immediate-trigger path. Test one request for typing → Done before debounce expires, and one fresh request for each explicit identical retry after success/failure.
- **AUD22** **S** **Verification — cover the Photos save outcome.** [ImageGalleryService](../ios/Packages/SystemServices/ImageGalleryService/ImageGalleryService.swift) now awaits `PHPhotoLibrary.performChanges` and reports `saveFailed`, so a denied or failed write reaches the existing alert path instead of a false success toast. No test covers it: [CollectibleViewModel](../ios/Features/NFT/Sources/ViewModels/CollectibleViewModel.swift) constructs the service inline, and the package declares no TestKit library (`ImageGalleryService/TestKit` holds only `.gitkeep`). Inject the saver the way `ConnectivityServiceTestKit` is injected, then test delayed success and failed/denied writes against the toast/alert mapping.
- **AUD23** **S** **Verification — cover websocket session retirement.** [Stream transport](../ios/Packages/SwiftHTTPClient/WebSocketClient/WebSocketConnection.swift) now invalidates the previous session before `startConnection` installs a new one, and `deinit` invalidates the last one, so a reconnect no longer strands a `URLSession` together with the `WebSocketSessionDelegate` it retains. No test covers it: `WebSocketClient` declares no test target in [Package.swift](../ios/Packages/SwiftHTTPClient/Package.swift) and the session is constructed inline, so neither creation nor invalidation is observable. Add a `WebSocketClientTests` target and the smallest seam that exposes session lifetime, then test that repeated failed connections retire every session and delegate, and that the connection-id guards still reject callbacks from a retired session.

### Android observation and recovery

- **AUD25** **S** **Bug — register preference observation before taking its snapshot.** [PreferencesStore](../android/data/services/gemstone/src/main/kotlin/com/gemwallet/android/data/services/gemstone/stores/PreferencesStore.kt) emits the initial value before attaching its listener. A selected-wallet write in that gap is missed, leaving session/screens/socket on null or the old wallet. Register first and preserve latest-value delivery. Test a deterministic write during subscription startup without requiring a second preference change.
- **AUD26** **S** **Bug — back off failed stream preparation.** [StreamObserverService](../android/data/services/gemstone/src/main/kotlin/com/gemwallet/android/data/services/gemstone/stream/StreamObserverService.kt) immediately loops after `prepareConnection` throws, outside the transport's reconnect delay. Persistent store/subscription failure can spin through IO, FFI and logging. Reuse the existing connection delay policy for preparation errors. Test bounded attempts with virtual time, reset after success and immediate cancellation.
- **AUD27** **M** **Bug — handle WebSocket callback overflow explicitly.** [WebSocketConnection](../android/data/services/gemstone/src/main/kotlin/com/gemwallet/android/data/services/gemstone/stream/WebSocketConnection.kt) ignores failed `trySend` for messages. Slow Core/storage handling can fill the buffer and silently lose normal or perpetual events. Preserve bounded ordered delivery or trigger explicit reconnect/resynchronization recovery; do not launch an unbounded task per message. Test overflow while consumption is suspended.
- **AUD28** **S** **Bug — do not mark an unsubscribed price stream healthy.** [Android stream observation](../android/data/services/gemstone/src/main/kotlin/com/gemwallet/android/data/services/gemstone/stream/StreamObserverService.kt) and [iOS stream observation](../ios/Packages/FeatureServices/StreamService/StreamObserverService.swift) report healthy before `connected()` finishes subscribing, then only log subscription failure. Report health after initialization and recover through the existing reconnect path; clear health when retiring the connection. Test failed first subscription followed by recovery and background cleanup on both apps.
- **AUD29** **M** **Bug — clear transaction details on wallet change or deletion.** [Transaction observation](../android/data/services/gemstone/src/main/kotlin/com/gemwallet/android/data/services/gemstone/stores/TransactionStore.kt) drops null rows; [detail projection](../android/data/coordinators/src/main/kotlin/com/gemwallet/android/data/coordinators/transaction/GetTransactionDetailsImpl.kt) combines session and transaction independently and drops null again. Old details/actions can survive or be paired with a different wallet. Preserve absence and bind the row to wallet identity. Test wallet A→B with no matching record and deletion of the displayed record; coordinate U18 without duplicating its model cleanup.
- **AUD30** **S** **Bug — surface failed preference commits.** [PreferencesStore](../android/data/services/gemstone/src/main/kotlin/com/gemwallet/android/data/services/gemstone/stores/PreferencesStore.kt) uses `edit(commit = true)` but discards the commit result for set/remove/clear. Core receives success even when durable persistence fails. Check the result and propagate through the existing store error contract, accounting for SharedPreferences' already-changed in-memory value. Test failed commits and restart-visible state; reuse the secure store's checked-write pattern.
- **AUD31** **S** **Bug — own add-node submission until it completes.** [AddNodeViewModel](../android/features/settings/networks/viewmodels/src/main/kotlin/com/gemwallet/android/features/settings/networks/viewmodels/AddNodeViewModel.kt) permits repeated submissions and applies completion to an editor that may have changed. Double taps can navigate twice; an old result can clear a newly entered URL. Use one submission job/in-flight state and reject obsolete completion. Test suspended submission plus repeated tap/input change, preserving the current Core validation session.
- **AUD32** **S** **Bug — bound support-image decoding.** [SupportImageAttachmentFactory](../android/features/settings/settings/viewmodels/src/main/kotlin/com/gemwallet/android/features/settings/settings/viewmodels/SupportImageAttachmentFactory.kt) decodes the full selected bitmap before compression and byte-array copying. Large photos can allocate hundreds of MB before upload. Inspect dimensions and downsample in this native adapter using existing upload constraints; release bitmap resources. Verify a large fixture produces a bounded, valid image. No measured OOM is claimed.
- **AUD33** **S** **Bug — scope activity refresh results to the active wallet.** [TransactionsViewModel](../android/features/activities/viewmodels/src/main/kotlin/com/gemwallet/android/features/activities/viewmodels/TransactionsViewModel.kt) publishes every refresh into one load state; wallet identity only protects `syncedWalletId`. A late wallet-A failure can replace wallet-B success, and overlapping jobs independently clear the spinner. Own the current refresh job/result and reset on wallet change. Test delayed A failure after B success and overlapping refreshes; `GemBalanceService`'s publication lane addresses a different persisted-balance race.

### Consolidation and regression coverage

- **AUD34** **M** **Consolidation — bound and share activity observation.** [GetTransactionsImpl](../android/data/coordinators/src/main/kotlin/com/gemwallet/android/data/coordinators/transaction/GetTransactionsImpl.kt) retains two maps for every visited filter, merges all historical maps per emission and starts a permanent default collector while screens collect additional cold queries. Its injected transaction service is unused. Consolidate ownership here, evict inactive filter state and share active observation. Preserve the three consumers' initial cached rows and row identity; test retained-state bounds and subscription counts after repeated asset/filter navigation.
- **AUD35** **S** **Verification — execute existing Room contract tests in CI.** [Android CI](../.github/workflows/android-ci.yml) only builds the app and runs JVM unit tests; [the existing integration command](../android/justfile) is separate. The real Room rollback/retry tests added for atomic pricing therefore have no CI execution path. Add a focused emulator job using existing instrumentation infrastructure, including persistence-contract tests as they land. Verify it executes nonzero tests and fails on broken transaction boundaries; MIG6 covers test content, not running it.

- **AUD36** **S** **Consolidation — delete the unused single-asset read stack.** [GetAssetById](../android/gemcore/src/main/kotlin/com/gemwallet/android/application/assets/cases/GetAssetById.kt), its coordinator and Hilt provider have no consuming code. Its sole store call is [AssetStore.observeAsset(AssetId)](../android/data/services/gemstone/src/main/kotlin/com/gemwallet/android/data/services/gemstone/stores/AssetStore.kt), also otherwise unused. Remove this stack together, retaining the separate NFT observation and shared DAO queries. Verify the symbol census and Android/Hilt compilation; do not replace it with another wrapper.
- **AUD37** **S** **Consolidation — trim unused legacy secure-store entry points.** [SecurityStore.putValue](../android/gemcore/src/main/kotlin/com/gemwallet/android/application/SecurityStore.kt) and [its Tink implementation](../android/app/src/main/kotlin/com/gemwallet/android/data/password/TinkSecurityStore.kt) have no callers; the Hilt binding also has no consumer. The active preferences adapter constructs the reader directly for legacy device-key migration. Remove the dead write API and binding while preserving reads and their internal migration writes/removals. Verify secure-storage migration tests and Hilt compilation; do not delete legacy recovery or introduce replacement storage.

### Repeated work and efficient execution

Reviewed at `46be74e99ddf9f1983f5ec079981e5b67832b0aa` on 2026-09-19. These are concrete repeated operations, not estimates of measured latency. Verify request, subscription, mapping or write counts for the named trigger, plus cancellation and failure behavior. Reuse data within the owning operation or screen; preserve independent work and later intentional refreshes.

- **AUD38** **M** **Consolidation — reuse the mode fetched for a perpetual refresh.** [`sync_positions`](../core/gemstone/src/services/perpetual/mod.rs) requests account mode alongside positions, while [HyperCore positions](../core/crates/gem_hypercore/src/provider/perpetual.rs) fetches the same `userAbstraction` again internally. Normal refresh and connection preparation therefore request the same address's mode twice and can use different snapshots for subscriptions and balance interpretation. Return the provider's mode with its positions result and reuse it in the service; retain the existing failure/fallback behavior explicitly. Verify one mode request per successful operation and consistent unified-mode output.
- **AUD39** **M** **Consolidation — share provider snapshots within one balance refresh.** [`chain_balances`](../core/gemstone/src/services/balance/mod.rs) launches separate component calls that repeat the same RPC: [Tron](../core/crates/gem_tron/src/provider/balances.rs) fetches the account for coin and staking; [Cosmos](../core/crates/gem_cosmos/src/provider/balances.rs) fetches bank balances for coin and tokens; [Stellar](../core/crates/gem_stellar/src/provider/balances.rs) fetches the account for coin and tokens. Extend the existing gateway/provider operation to fetch each shared snapshot once and derive the requested components, keeping independent calls concurrent. Test RPC counts, exact values and partial failures for these providers; preserve the balance publication lane's ordering contract. No global response cache.
- **AUD40** **S** **Consolidation — share perpetual-market observation before branching.** [PerpetualMarketViewModel](../android/features/perpetual/viewmodels/src/main/kotlin/com/gemwallet/android/features/perpetual/viewmodels/PerpetualMarketViewModel.kt) separately collects the same cold market flow for pinned/unpinned lists, duplicating Room observation and full row formatting. Its display balance and withdrawal eligibility also independently collect the same balance query. Share each source once within the existing screen lifetime and derive its outputs, following wallet search's shared flow. Verify one upstream collection and mapping pass per query/emission with both outputs observed; retain search/wallet replacement. Coordinate balance presentation with R98.
- **AUD41** **S** **Consolidation — key perpetual observation by stable identity.** [PerpetualDetailsViewModel](../android/features/perpetual/viewmodels/src/main/kotlin/com/gemwallet/android/features/perpetual/viewmodels/PerpetualDetailsViewModel.kt) keys socket setup by the complete mutable market, so price/volume emissions cancel and repeat identical unsubscribe/subscribe calls. They also restart its position query. Compare the existing Core-produced subscription identities before collection; key the position query by wallet and perpetual id. Test zero additional subscription calls/query starts for price-only changes, with correct replacement and cleanup for actual period/market/wallet changes. S73/S81 remain responsible for chart/load refresh behavior.
- **AUD42** **S** **Consolidation — reuse simulation's asset-existence result.** [`ensure_simulation_assets`](../core/gemstone/src/services/assets/mod.rs) reads stored ids and computes missing ids, then passes those ids to `sync_missing_assets`, which repeats the same existence read/calculation. Reuse the first result with the existing API fetch/save operation inside this owner; retain node fallback for unresolved assets and the final asset read. Test one existence read and one backend fetch for an uncached simulation token, including backend failure/partial results. Keep persistence idempotent; this is separate from K18's unchanged-row writes.
- **AUD43** **S** **Consolidation — replace the wallet-search request once per edit.** [iOS wallet search](../ios/Features/WalletTab/Sources/ViewModels/WalletSearchSceneViewModel.swift) assigns `searchBy`, `searchKey` and `limit` separately. Each changed field restarts [ObservableQuery](../ios/Packages/Store/Sources/Query/ObservableQuery.swift) and immediately fetches, producing two or three reads and intermediate request combinations for one edit. Build the final request locally and assign once. Verify one replacement observation/fetch with all final fields and no intermediate result emission; keep native observation.
- **AUD44** **S** **Correctness — prefetch receive associations on the paths that skip the asset screen.** [`GemReceiveService`](../core/gemstone/src/services/receive/mod.rs) no longer syncs associations; [`GemAssetDetailsService::refresh`](../core/gemstone/src/services/assets/details.rs) already calls `sync_asset_associations`, which fetches the asset and its missing associated assets. Receive reached from the wallet-tab select-asset sheet ([SelectAssetSceneNavigationStack](../ios/Gem/Navigation/Assets/SelectAssetSceneNavigationStack.swift), [Receive.kt](../android/app/src/main/kotlin/com/gemwallet/android/ui/navigation/routes/Receive.kt)) or the scan modal never runs that refresh, so its network selector shows whatever associations the store last held. Prefetch in the select-asset flow's existing Core refresh, not in the receive service. Test that a token with unsynced associations offers every wallet network on both entry paths.
- **AUD45** **M** **Consolidation — give confirm-transfer one view state instead of four stored records.** [ConfirmTransferSceneViewModel](../ios/Features/Transfer/Sources/ViewModels/ConfirmTransferSceneViewModel.swift) stores `button`, `feeRow`, `feeRates` and `rowContents` and re-derives all four at two sites, with a `didSet` re-deriving `feeRates` on its own. That is the second writable view state [A screen's state is one phase enum](ARCHITECTURE.md#a-screens-state-is-one-phase-enum-never-a-bag-of-flags) and [One `view_state` call returning one record](ARCHITECTURE.md#a-screen-whose-state-changes-is-a-session) forbid: four fields that can disagree after a partial refresh. Return one record from the existing confirm screen owner and derive it once per change, following the fiat session's `view_state`. Mirror the same single record on Android. Test that a fee-rate change and a refresh each produce one consistent state with no intermediate combination.
- **AUD46** **S** **Consolidation — move the autoclose default-vs-edited rule to its session.** [AmountPerpetualViewModel](../ios/Features/Transfer/Sources/ViewModels/AmountPerpetualViewModel.swift) keeps `takeProfit`, `stopLoss` and a private `isAutocloseEdited` flag, and uses that flag to decide whether a leverage change may overwrite the user's prices. [`GemAutocloseSession`](../core/gemstone/src/services/perpetual/autoclose.rs) already models autoclose fields, validation and submission for the edit screen. Let the open-position screen carry the same session so the rule lives once; keep the defaults projection where it is if the session would otherwise need a service. Test that a leverage change refreshes untouched defaults and preserves edited prices on both apps.

## Consistency sweep: naming, services, rows and sessions

Surveyed on 2026-09-21. Each item names what was counted and where; confirm the count before working one, since the apps move. These are consolidation and naming items, not defects: nothing here is user-visible on its own, and the value is that one decision keeps one name and one owner across Core, iOS and Android.

### Names

- **N14** **M** **The contact editor is named `ManageContact` end to end.** Core exports `GemManageContactService` from `services/manage_contact/`, and both apps follow: iOS `ManageContactViewModel` with `manageContactModel(mode:)`, `manageContact(for:)` and `manageContactScene(mode:)`, Android `ManageContactViewModel` and `ManageContactNavScreen`. `manage` is one of the verbs [Engineering Principles](../skills/engineering-principles.md) rejects, and the screen adds or edits one contact. Rename the Core service and module with both apps' types in one commit, so the three app functions can say what they build; a half-rename leaves the apps and Core disagreeing.

### Services

- **N4** **M** **iOS screens holding two Core services.** [ARCHITECTURE § 7](ARCHITECTURE.md#7-at-most-one-core-service-on-ios-narrow-cases-on-android) allows one. Eight hold two: `RecipientSceneViewModel` and `ManageContactViewModel`/`ManageContactAddressViewModel` (name beside the screen service), `AmountSceneViewModel` (amount and stake), `PerpetualsSceneViewModel` and `SelectAssetViewModel` (recent activity beside the screen service), `ImportWalletViewModel`/`ImportWalletSceneViewModel` (name beside wallet). Each second service is a composition service the screen service should reach, per [Composition services are reached through the screen service](ARCHITECTURE.md#composition-services-are-reached-through-the-screen-service). Move the calls behind the screen service one screen at a time; the name service is the common case, so do a recipient screen first as the exemplar.

### Rows and records

- **N7** **L** **Android restates Core records as aggregates.** `android/data/coordinators` holds seven `*AggregateImpl` classes — transaction data and details, wallet summary and details, perpetual details and position details, price alert — that unpack a Core record field by field. U15 showed the cost: `TransactionDetailsValue.SwapProgress` renamed `etaSeconds` to `etaInSeconds` on the way through, so the two drifted silently. Hold the record and read through it, as `WalletDetailsAggregateImpl` now does. U18 covers the transaction one; this is the rest.
- **N8** **M** **Rows assembled app-side that Core already decides.** R104 found the rewards stats list built twice with different contents. Sweep the remaining `*UIModel` builders (44 files under `android/features`) and the iOS `*ListItem`/`*ViewModel` row getters for lists Core could return as `GemListRow`, and file the ones that differ between apps. Start where a list is built on both sides: settings sections, asset detail rows, perpetual position rows.

### Sessions

- **N10** **M** **Screens with domain state and no session.** Core has 18 `Gem*Session` types; iOS references a session in 17 view models and Android in 39, so adoption is uneven rather than absent. Audit the screens that hold several mutable domain fields without one against [A screen whose state changes is a session](ARCHITECTURE.md#a-screen-whose-state-changes-is-a-session), and remember the exclusions: one text field, one selection, navigation state or a settings mirror does not need one. AUD45 and AUD46 are the two already confirmed; this is the sweep that finds the rest.
- **N11** **S** **Sessions used on one platform only.** Where Core exposes a session, both apps should drive it. S75 records the open-position autoclose sheet bypassing `GemAutocloseSession` on Android; check the other 18 for a platform that hand-rolls the same transitions, and file one item per session rather than a single sweep.

### Generated surface

- **N12** **S** **Keep the FFI surface swept.** U36 removed 26 types that reached no exported signature, cutting 2,186 lines of generated Swift. The surface grows back: after adding an export, check with `cargo build` that removing its derive fails, and re-run the U36 sweep each release rather than letting it accumulate.

## Correctness first

Found on 2026-09-19 and verified in the code: transaction-critical input, parsing that changes a shown amount, crashes, and regressions the earlier row migrations introduced. Work these before anything below.


- **X172** **M** `GemKeystore` exports four raw secret operations production never calls (`preview_import`, `create_store`, `export_recovery_phrase`, `export_private_key`; `keystore/keystore.rs:36,55,80,90`); production goes through `GemWalletService::import_wallet`/`export_secret`, yet both apps hand the keystore object around (`LocalKeystore.swift:6` public, Android Hilt `WalletsModule.kt:39`). Move the four into the plain `impl` block and point the test kits at the service. The test kits lean on the four: iOS `LocalKeystore+Keystore.swift` and `LocalKeystore+Export.swift`, Android `GemstoneTestKeys.kt`, and the keystore concurrency, benchmark and migration instrumentation tests, so each moves to the service or a Core test first.

## 0. Duplicated code to delete first

This bucket groups duplication found by the 2026-09-19 review; the execution order above prioritizes correctness and actual dependencies over line-count savings. The older families are in the ledger.

- **U9** **M** Each app keeps a second number renderer beside `GemFormattedNumber.text()`: iOS `CurrencyFormatter.swift:33-47` and `PercentFormatter.swift:16-23`, Android `CurrencyFormatter.kt:26-49` and `PercentageFormatter.kt:19-40` (the percent `DecimalFormat` copied line for line). Android `CurrencyFormatter(Abbreviated)` rounds down where the renderer rounds half-even, and `CurrencyFormatter.Type` twins `GemCurrencyStyle` (20 uses); Core's `formatted_currency/amount/adaptive` exports have only test-kit callers, and Android `ValueFormatter.string` makes three FFI crossings per value. Route f64 formatting through `formattedCurrency(...).text()` plus a percentage export and delete the duplicated compact, percent and dust setup and `Type`; the exact-amount path stays app-side (R86).
- **U17** **S** The price widget decides the change tone itself and disagrees with the app on zero: [WidgetPriceService](../ios/GemPriceWidget/Services/WidgetPriceService.swift) and [PriceWidgetEntry](../ios/GemPriceWidget/Types/PriceWidgetEntry.swift) store `changeIsPositive: change >= 0`, so [CoinPriceRowViewModel](../ios/GemPriceWidget/ViewModels/CoinPriceRowViewModel.swift) paints a flat 0% change green while `GemValueTone::of(0.0)` is Neutral and the app paints it grey. Read Core's `valueTone(value:)` instead. The blocker is linkage, not code: the GemPriceWidget target's package dependencies are Style, Primitives, Formatters, Components, SwiftHTTPClient and WidgetLocalization, so it can reach neither `Gemstone` nor the `GemValueTone.color` map that lives in [PrimitivesComponents](../ios/Packages/PrimitivesComponents/Sources/Extensions/Gemstone+Style.swift). Adding one is a project-file change and pulls the Gemstone binary into a widget extension, which has its own memory budget, so weigh that before moving the colour map down. The earlier `valueTone(value:)` re-derivation and the private colour map this item described at `:52-67` are already gone.
- **U18** **M** Android restates the transaction details record: `GetTransactionDetailsImpl.kt:70-185` rebuilds `GemTransactionDetailRows` as `TransactionDetailsAggregateImpl` with unread members and a five-subclass participant twin (`TransactionDetailsValue.kt:51-90`) that drops `can_add_contact` and `name.type`; iOS reads the record directly. Build the row UI models from the record and delete the aggregate, its implementation and the twin.
- **U19** **M** Both apps run the payment flow step by step (`load` → `transaction_asset_id` → `ensure_token_asset` → `transaction_transfer_data`: `NavigationRouter.swift:176-184`, `PaymentNavigation.kt:34-52`), and on a request `Confirm { asset_id }` each app looks the asset up and crosses again (`PaymentDestinationBuilder.swift:32-70`, Android `PaymentDestination.kt:32-53`, a twin of `GemPaymentDestination`). The payable set differs: iOS passes every wallet asset and account, hidden ones included, Android only visible assets from a StateFlow seeded empty, so a request for a hidden token confirms on iOS only. `GemPaymentService::prepare(payment, wallet) -> GemPaymentDestination` reads the wallet's assets and accounts and returns the finished `GemTransferData`; delete the builders, `PaymentDestination` and the app asset reads. Keep the explicit wallet and existing decoder/transfer records. Add only genuine owning-service dependencies after a cycle check: recipient already depends on payment, so payment must not depend back on recipient or move app storage into the decoder crate.
- **U25** **M** The balance total formula is written in app language three times beside Core's `GemAssetBalance::total`: [iOS `Balance.total`](../ios/Packages/Primitives/Sources/Extensions/Balance+Primitives.swift) and Android's two [`getTotalAmount`](../android/gemcore/src/main/kotlin/com/gemwallet/android/model/AssetBalance.kt) overloads. These are the copies that can drift from Core and should go. The two SQL copies are the stored projection the item allows and should stay: iOS computes `totalAmount` as a SQLite generated column ([BalanceRecord](../ios/Packages/Store/Sources/Models/BalanceRecord.swift)), which cannot go stale, and Android sums the components in its `updateBalance` UPDATE ([BalancesDao](../android/data/services/store/src/main/kotlin/com/gemwallet/android/data/service/store/database/BalancesDao.kt)). The work is threading that stored total through `AssetData` on iOS and `AssetInfo` on Android so the five callers — both `PortfolioStore`s, `AssetDataViewModel`, `AssetInfoUIModelFactory` and `AssetBalance.kt` — read it instead of summing again; that is why this is M, not S. Do not add a total to `GemBalanceRecord`: neither store would write it, since both already derive it in SQL. Verify component totals and non-USD valuation, and note the two SQL shapes differ (generated column vs UPDATE) under K21.
- **U26** **S** Dead app code, Android remainder: [`AssetsDao.getAssetsInfoByAllWallets`](../android/data/services/store/src/main/kotlin/com/gemwallet/android/data/service/store/database/AssetsDao.kt) takes a `walletId` its query never uses and is read only by [Migration_71_72Test](../android/data/services/store/src/androidTest/kotlin/com/gemwallet/android/service/store/Migration_71_72Test.kt), to assert another wallet's asset survives the migration with no balance row. Delete the query and keep that assertion, rewriting it as raw SQL beside the `balances` assertions the same test already makes. Do it when instrumented tests can actually run (X172, AUD35), so the rewritten assertion is verified rather than assumed. The iOS half of this item is done.
- **U33** **M** App tests restate Core rules and fail when a Core rule flips: swap (minimum offered only when the balance covers it, insufficient balance before a quote, retryable failures; iOS `SwapSceneViewModelTests.swift:458-485`, Android `SwapViewModelTest.kt:649-704`), fiat (`FiatSceneViewModelTests.swift:103,190`, `FiatViewModelTest.kt:207-338`), amount and price alert (`AmountSceneViewModelTests.swift:70,90`, `SetPriceAlertViewModelTests.swift`, `AmountViewModelTest.kt:92,184`, `AmountTransferProviderTest.kt`), iOS `PaymentTransferTests.swift` and `ConfirmTransferSceneViewModelTests.swift:481,494`, Android `CandleTooltipTest.kt`, `EquivalentValueTest.kt` and `SwapDetailsUIModelFactoryTest.kt:135-160`. Move missing domain cases into Core and delete tests that only restate those decisions through mocks; retain meaningful mapping, wiring, navigation and adapter coverage. Retain real adapter, navigation and meaningful integration invariants; a test crossing Core is not automatically a duplicate (see MIG6 and ARCHITECTURE § 10).

## 1. Surfaces still outside Gemstone

Rows, headers, screen state and flows that an app still assembles from Core ingredients ([the record carries the finished value](ARCHITECTURE.md), [a plain list is Core sections of one shared row](ARCHITECTURE.md#a-plain-list-is-core-sections-of-one-shared-row-rendered-by-one-builder-per-app)). Where the two apps already differ, the item says how.

- **C51** **L** The confirm header is composed twice: iOS `ConfirmHeaderViewModel.swift:47-71` with `TransactionInputViewModel.swift:40-81` (and `TransactionHeaderBuilder.swift:25-51` JSON-encoding `TransactionSwapMetadata`), Android `ConfirmHeaderUIModel.kt:35-59` with `ConfirmViewModel.kt:215-241` and `AmountUIModel.kt`. They differ for a token approval (image vs symbol), before the load (requested amount vs an empty head), for a generic request with a simulation header, and for a failed payment request, and both swap a withdrawal to the HyperCore deposit asset. `GemConfirmation::header(load)` returns `Placeholder | Simulation | Transaction(GemTransactionHeader)`, rendered through the existing transaction-header mappers.
- **C52** **L** Deep links and pushes prepare their target differently: iOS `NavigationRouter` runs `openAsset` for an asset link, `ensureAsset` for receive/buy/sell/swap, routes perpetual assets to the perpetual scene and passes `rewards("")` through; Android routes links with no Core call (`WebDeepLinks.kt:14-23`), buy/sell through `openAsset`, pushes through `openAsset`/`ensureAsset`, sends fiat and stake pushes to `AssetRoute` always, and turns a blank code into null. A Core `open(action) -> GemNavigationTarget` prepares the assets and switches the wallet once; the apps map the target to a route.
- **C53** **M** Wallet import and create are orchestrated on both apps (default name, `import_name`, `import_request`, `import_wallet`, `set_current_wallet_id`, accept terms): Android computes the default name when the screen opens, so an import tapped before that finishes is named `""` (`ImportScreen.kt:183,194`), and makes a created wallet current before setup where iOS does it after. One Core import (and create) call that names, stores and activates the wallet.
- **S72** **M** The Android asset chart discards its `GemChartSession` on every load (`ChartViewModel.kt:49-71` builds `newSession().onSelectPeriod` per trigger), so a pull-to-refresh blanks the chart, a failed refresh replaces a loaded chart, `Failed` loses its error, while iOS keeps one session. Android already injects `GemChartServiceInterface`; the remaining issue is state ownership, not its production service type. Hold the session, drive the iOS events, delete `ChartRefreshController`. Extend result applicability beyond period alone where overlapping loads are possible: test A→B→A and currency changes, with a concrete request identity/generation on the existing session rather than a new state framework.
- **S73** **M** The perpetual candle chart's state is hand-built on both apps: iOS draws an empty series as data and replaces candles with an error, Android emits Loading on every resume and after each confirm and shows an error without text. A candle session on `GemPerpetualDetailsService` with the `GemChartSession` rules. Carry market/period/currency request identity and reject stale successes and failures; reuse the shared load policy and preserve displayed candles during refresh failures.
- **S74** **M** The portfolio screen has no session: per-type state, period, chart type, "fall back to the first period" and the hardcoded period list live on both apps (`PortfolioSceneViewModel.swift:31-106`, `PortfolioChartViewModel.kt:74-130`, `WalletChartPeriods.kt`), and Android reloads on every tab switch and drops errors. `GemPortfolioSession`. Include wallet/type/period/currency in result identity and test delayed results after selection changes. Reuse shared load/number/list infrastructure; coordinate R94 and O59.
- **S75** **M** Android's open-position autoclose sheet bypasses `GemAutocloseSession`: `AmountAutocloseSheet.kt:63-132` decides validity, confirm and the ROE target inside the composable, the provider shows size as amount × leverage ($1,000 where iOS shows the $100 margin at 10x) and validates against the live price where iOS uses the snapshot. A view model on the Core session.
- **R89** **M** Confirm error info sheets are composed twice (iOS `ConfirmInfoSheetBuilder.swift:40-75` and `InfoSheetModelFactory.swift`, Android `ConfirmErrorUIModel.kt:47-123`); "network fee required" shows the requirement on iOS and the loaded fee on Android (`:72`), and the "amount (~fiat)" template exists three times. `GemConfirmation::error_info(error)` with required/available/shortfall as `GemFormattedNumber` plus fiat and the acquire action.
- **R90** **M** Confirm recipient row content differs: a contract's name shows on iOS (`ConfirmRowViewModel.swift:53`) and not on Android (`ConfirmRowUIModel.kt:61-68`), a provider row is tappable on iOS only, iOS `.nameOrAddress` bypasses `name_text`, and initials are `prefix(2)` on iOS and `contactInitials` on Android. A finished recipient row (name text, avatar, copy, link, selectable), with `with_address_name` deciding contract naming.
- **R91** **M** Swap details rows are keys each app fills (iOS `SwapDetailsViewModel.swift:75-167` and `PriceImpactViewModel.swift`, Android `SwapDetailsUIModelFactory.kt:61-106` with two price-impact colour maps): manual slippage shows the selected value on iOS and the quote's on Android, and with no rate Android drops the whole details model. The summary takes the prices and returns `GemListRow` sections with price-impact and slippage info topics; the rate stays a rich row.
- **R94** **M** Portfolio statistics cross as bare f64 and each app builds the rows (`PortfolioSceneViewModel.swift:121-168` with `AllTimeValueViewModel.swift`, `PortfolioStatisticListItem.kt` with `AllTimeListItem.kt`) with different date formats, an iOS-only dash and the margin "(percent)" template twice, while Core already builds the chart's `GemListRow::AllTime`. Statistics as `Vec<GemListRow>`.
- **R95** **M** The wallet header total and PnL cross as `TotalFiatValue`, three f64s (`wallet_home/mod.rs:20-27`): Android clamps a non-positive total to $0 when PnL is hidden (`GetWalletSummaryImpl.kt:96-129`) and iOS prints it raw, the PnL tone comes from the amount on iOS and the percent on Android, and Android writes "(percent)" in a composable (`AmountListHead.kt:177`). `total: GemFormattedNumber` and a PnL value on the view state; delete `TotalValueViewModel` and `buildWalletSummaryDisplayState`.
- **R96** **M** The perpetual position list row formats margin as adaptive currency on iOS and two-decimal fiat on Android ($0.5432 vs $0.54) and composes PnL through `PnLViewModel`/`formatPnlWithPercentage` (`PerpetualPositionViewModel.swift:47-75`, `PerpetualPositionDataAggregateImpl.kt:19-33`). The row carries `margin: GemFormattedNumber`, `GemLocalizedText::Pnl` with a tone and the position label.
- **R98** **S** The perpetual deposit and withdraw buttons route differently: iOS opens select-asset (`PerpetualsScene.swift`), Android goes straight to the amount screen (`PerpetualMarketNavScreen.kt`). Pick one and give `GemHeaderButtonKind::Deposit`/`Withdraw` a single destination; the header record itself is now Core's (`perpetual_balance_header`).
- **R100** **M** Delegation list rows still differ: iOS `DelegationViewModel` calls the stake service per row; Android `BalanceInfoUIModel` converts exact crypto values to `Double` and greys zero balances, and validator-row mapping is repeated in stake/earn. Add a batched `delegation_list_rows` projection at the existing stake owner (`delegation_rows` already names detail rows). Reuse validator records and rich row components; preserve exact BigInt values with decimals/display policy, using `GemFormattedNumber` only for approximate fiat/APR fields. Delete repeated app decisions and test small/high-precision rewards.
- **R101** **M** Delegation details: the title is Stake or Earn by provider on iOS and always Stake on Android (`DelegationScene.kt:43`), the header and rewards use short vs auto precision, and both apps build the claim `StakeType.Rewards` transfer. `GemStakeService::delegation_details`.
- **R102** **M** Asset details: `GemAssetDetailRow::Price` carries nothing, so a zero price reads "$0.00" on iOS and "" on Android; the earn row is green with an emoji on iOS and plain on Android; the header balance is formatted app-side although Core has `balance_amount`. `Price { price, change }`, the earn row as the toned `earn_apr_row`, and `GemAssetDetails.balance_value`.
- **R111** **M** Asset row titles and symbol badges are computed per app: the network title is `chain.networkName` on iOS and `chain.asset().name` on Android ("TON" vs "Gram"), Android select hardcodes `CANONICAL_ASSET` against the flow's title style (`BaseAssetSelectViewModel.kt:134`, `PriceAlertViewModel.kt:70`), and the hide-the-symbol rule compares different names on each app although Core has `GemAssetText.subtitle_symbol`. One Core projection per list; fix the ARCHITECTURE example.
- **R112** **S** Android never masks position margin and PnL when balances are hidden: `isBalanceHidden` reaches only [AssetsHead](../android/features/assets/presents/src/main/kotlin/com/gemwallet/android/features/assets/views/components/AssetsHead.kt), so the wallet-home perpetual rows and the asset rows beside them keep showing their values while iOS's `PrivacyText` hides them. Android needs row-level masking before this closes; the preview row itself now reads the same title and Core balance on both apps.
- **R113** **S** Banner presentation is decided per event on both apps (close-ability, welcome layout vs list, Buy/Receive actions, the amount style: `BannerViewModel.swift:44-118`, `BannerRowUIModel.kt:42`, `BannersScene.kt:59-60`, `WelcomeBanner.kt:67-78`). `GemBannerContent` gains `can_close`, `style`, `actions` and the fee as a `GemFormattedNumber`.
- **R114** **M** NFT list screens differ in title, receive action and refresh scope: iOS uses the collection name, Android "Collections"; the iOS Collection/Unverified empty-state action does nothing. iOS automatic sync is root-only; Android `syncIfNeeded` skips Collection but explicit refresh syncs it (`NftListViewModels.kt:76-98`). Extend the existing NFT list-screen answer with title/action and event-specific refresh scope using `GemNftList`; keep native grid/list rendering. Test root, collection and unverified initial/pull behavior with S80's load state.
- **R118** **S** Stake action destinations: iOS `StakeSceneViewModel.swift:157-164` and Android `StakeActions.kt:28-39` (in a composable) switch `GemStakeAction` to amount inputs with a Bandwidth default, and the resource options are hardcoded on both (`AmountStakeViewModel.swift:38`, `ProviderExtras.kt:82`). `GemStakeActionItem.destination` and resource options on the amount input; delete the Android `StakeAction` twin.
- **S79** **M** The rewards screen hand-rolls its load state and loses the failure and the wallet: a failed `getRewards` becomes `.noData` on iOS and `state(null)` on Android (`RewardsViewModel.swift:300-307`, `ReferralViewModel.kt:102-110`), so offline both show the create-code screen to a user who has one; a wallet switch starts a second uncancelled load on both; Android's `createReferral`/`useCode`/`redeem` swallow cancellation. `GemRewardsService::refresh(wallet, previous)` carrying a `GemLoadState` and the wallet id. Accept results only for the active wallet/request; returning a wallet id without enforcing acceptance is insufficient. Coordinate the actions and rows in R104/R123 after this state contract.
- **S80** **M** Five store-backed screens show their empty state when the first sync fails, logging Core's error on both apps: NFT collections, fiat transactions, price alerts, in-app notifications and support chat (`CollectionsViewModel.swift:45-52`/`NftListViewModels.kt:95-98`, `FiatTransactionsViewModel`, `PriceAlertsSceneViewModel.swift:66-72`/`PriceAlertViewModel.kt:108-124`, `InAppNotificationsViewModel`, `SupportChatSceneViewModel`). Each sync becomes `refresh(has_rows) -> GemLoadState`, the D40 shape.
- **S81** **S** Perpetual refreshes run different syncs: iOS full load/pull updates positions, transactions and candles, while activation updates transactions and candles (`PerpetualSceneViewModel.swift:157-184`); Android syncs transactions once per view model and subsequent refreshes update positions/candles (`PerpetualDetailsViewModel.kt:104-109,239-250`). Define event-specific refresh work in the existing `GemPerpetualDetailsService` and return structured step failures; both native lifecycle triggers call that answer. Verify that later take-profit/liquidation history appears without reopening and that unrelated requests remain concurrent.
- **S82** **S** Only iOS refreshes the swap pair's balances (`update_balances` and `add_prices` on each pair change, `SwapSceneViewModel.swift:317-321,496-510`); Android never calls `update_balances`, so max, percent and the insufficient-balance check run on whatever balance was last stored. One Core step for "the pair changed".
- **S83** **S** Fiat quote polling runs on different clocks: iOS restarts the timer per request and stops it off-screen (`FiatScene.swift:41`), Android's `tickerFlow` ticks from view-model creation and keeps fetching while the provider page is open or the app is backgrounded (`FiatViewModel.kt:148-170`); both poll a failed phase. `GemFiatSession::refreshes_quotes(is_screen_active)` as swap has.
- **R128** **S** The selected chart point's date is chosen by period on iOS only (`ChartDateFormatter.swift:22-28`: relative for hour/day, date and time for week/month, date for year/all); Android always uses the relative date (`ChartUIModel.kt:35-40`, `PerpetualChartSection.kt:53-57`). The chart header record carries the date style.
- **R129** **S** A literal "%" is appended to Core numbers in more places: slippage suggestions and limits (`SlippageSuggestion.swift:10-12`, `SwapSlippage.kt:9`, `SlippageStateUIModel.kt:23-24`) and the iOS swap amount chips (`PercentageSuggestion.swift`, the legacy `init(value:)`), wrong for fr and tr; Android's percent renderer also replaces the locale's prefix, so Turkish "%5,23" loses its sign (`FormattedNumber.kt:47-60`, `PercentageFormatter.kt:27-37`). Percent `GemFormattedNumber`s and U9's single renderer.
- **R131** **S** Untranslated text, remaining two: `GemListRowTitle.Stream` renders as the literal "Stream" in both mapper files ([iOS](../ios/Packages/PrimitivesComponents/Sources/Extensions/Gemstone+Localized.swift), [Android](../android/ui/src/main/kotlin/com/gemwallet/android/ui/localization/GemstoneText.kt)) — the shape is right after service status moved into Core, the string just needs a key — and Android's hardcoded "Validators not found" fatal screen where iOS shows an empty list ([ValidatorsScreen.kt](../android/features/transfer_amount/presents/src/main/kotlin/com/gemwallet/android/features/transfer_amount/presents/ValidatorsScreen.kt)). Both need new keys, and `localization/app/` has 31 language files that each require a translation for the context, so this runs through [translation review](../.agents/skills/translation-review/SKILL.md) rather than being written blind. The validators screen also needs a validators kind in [`GemEmptyStateKind`](../core/gemstone/src/services/empty_state.rs), which has none today. The iOS `Section("Chains")` header is done: it reuses the existing `settings_networks_title`.
- **R133** **M** Asset list rows compute balance and fiat app-side: Android sums per-field `Double`s and multiplies `double * price` (`AssetBalance.kt:48-75`, `AssetInfoDataAggregate.kt:41-68`), iOS formats the BigInt and converts through `to_fiat` (`BalanceViewModel.swift:47-66`), while Core's `balance_amount` and `fiat_value` serve only asset details. Extend R111 with a batched list-row projection carrying the Core-selected values and display policy. Preserve exact BigInt crypto values plus decimals where precision is required; use the f64 `GemFormattedNumber` for approximate fiat, not as a replacement for exact amount transport.
- **P97** **S** A failed push toggle has inconsistent feedback/state handling: iOS retains the attempted toggle and alerts, Android rereads `notificationsService.isEnabled()` and logs (`NotificationsViewModel.swift:38-40`, `DevicePushSettings.kt:50-58`), while Core may already have persisted the preference. Return the resulting state and failure through the owning service, distinguishing stored preference, OS permission and remote registration; retry must repair unfinished synchronization, the way `GemPriceAlertService::set_enabled` now re-attempts the device sync on an identical toggle. A failed banner close also toasts on iOS and only logs on Android; surface it through the existing error/toast path.

## 2. Decisions still made twice

The same product rule on both apps with a difference, each read on both sides on 2026-09-19.

- **D55** **S** A below-minimum amount opens a minimum-amount sheet with Buy on iOS (`AmountSceneViewModel.swift:228-236`) and is text only on Android, and "only NoQuote has a sheet" is decided per app for swap. `info()` on `GemAmountErrorDisplay` and `GemSwapErrorDisplay`.
- **D56** **S** Wallet search: Android's empty state counts only some sections and draws "no assets" over matching lists, iOS `showAddToken` bypasses `flow.shows_add_token`, and pinned perpetuals vanish from iOS asset-list results. A Core search state from the full counts; both apps honour `shows_add_token`; one perpetual split.
- **D58** **S** A support message interrupted by an app kill is failed on resume on Android (`failPendingSupportMessages`, outside the `GemSupportStore` trait) and spins "sending" forever on iOS, and Android restates Core's no-retry-for-images twice. `GemSupportStore::fail_pending_messages` from a Core entry point and message rows with a status outcome. Run interrupted-send recovery from the existing startup/resume lifecycle after identifying abandoned work; do not fail actively sending rows on every refresh. Reuse existing message status and retry rules.
- **P90** **S** The token-approval header is image-only on iOS but drawn as the symbol on Android transaction details (`GetTransactionDetailsImpl.kt:99-100`) and confirm (`ConfirmHeaderUIModel.kt:53`), merging Core's `AssetImage` into `Symbol`. Close the confirm half with C51 and the detail half with U18; do not add a third header mapping.
- **D66** **S** Perpetual market membership differs: iOS takes the top 100 by volume and derives pins from that capped set (`PerpetualsRequest.swift:8,28`); Android excludes zero volume except on priority search (`PerpetualDao.kt:34`, `PerpetualStore.kt:84-99`). Define listed/tradable eligibility in Core and a consistent query contract, read pins independently of the result cap, and retain native indexed search/order. Keep delisted metadata needed for held positions/history; do not delete markets just to hide them from discovery. Test pinned-outside-cap, zero-volume search and a held delisted position.
- **D67** **S** Swap's default pay asset depends on adapter candidate order: iOS uses enabled swappable candidates with pins first, Android uses a capped visible nonnegative-rank list and re-sorts away pinned precedence; recent limits/filtering also differ (20 versus 10). `GemSwapService::suggest_pair` already owns selection. Define one eligibility, ranking and limit contract on its existing store input, implement it in both indexed queries, and remove redundant app re-sorting. Do not load/sort an unbounded wallet in Core. Test hidden/enabled, pins, caps and recents with paired query fixtures.

## 3. The view boundary

[ARCHITECTURE.md § 5](ARCHITECTURE.md#a-view-never-names-a-core-type): feature views consume prepared UI values and do not call Core, choose localized wording from domain cases or reconstruct domain decisions. Shared row renderers and opaque row-key dispatch are intentional exceptions. Move interpretation into the model/mapper; do not add a wrapper solely to hide a generated type name. B67 records an earlier census; inspect each current hit for the actual behavior described below.

- **B76** **M** Asset and chart screens decide from Core types again: `AssetScene.swift:97-174` switches `GemAssetDetailRow`/`GemAssetNetworkDestination`/`GemAssetBalanceRow` (added by b15426c878), `ChartScene.swift:21-48` switches `GemChartSection`, and Android `AssetDetailRowItem.kt:76-81`, `AssetDetailsScene.kt:52-76` (routes on `GemListRowTitle` and picks toasts), `AssetSelectScreen.kt:54-87` with `AssetSelectFlowUIModel.kt:9-23`, `AssetInfoUIModel.kt:35-38`, `BannerItem.kt:17` and `GemLineChart.kt:82`. The B67 shapes.
- **B77** **M** Settings, referral and support views name Core types: `ReferralScene.kt` takes `GemRewardsState` and branches on eight flags (7f4d7b0741), `SecurityScene.kt:57-58`, `PreferencesScene.kt:45,119,130`, `AboutUsScreen.kt` calls `aboutSections` in the composable with no view model, and iOS `SupportMessageBubble.swift:101` takes `SupportMessageLink`. A referral UI state, a `securityAction()` mapper, an About view model and an iOS support link row model. The About view model is also what Android needs to reach the developer preference, so the toggle can move off the Settings About Us row (`SettingsScene.kt:83-105`, `opensDeveloperMenu()`) onto the About version row where iOS keeps it.
- **B78** **M** The Android amount screen has no UI state: `AmountScreen.kt:74,76` calls Core in the composable, `ProviderExtras.kt:32-128` receives the provider and branches on `AmountStakeProvider`/`AmountParams.Stake.*`, and `AmountErrorText.kt:9-14` checks `GemAmountException` in a composable. `AmountUiState`.
- **B79** **M** Android's wallet screens hand `GemWalletRow` to composables through two app aggregates (`WalletDataAggregate`, `WalletDetailsAggregate`, the latter restating `GemWalletDetails`), and the composables map and decide from it: `item.row.uiModel(LocalContext.current)`, the pin label from `row.isPinned`, the pinned split and `row.placeholder.iconModel()` (`WalletItemsList.kt:38-56`, `WalletsScreen.kt:84`, `WalletScene.kt:120-131`); the census missed them because no Core type is named. The cases return the records and the view models emit row UI models; iOS already vends the `ListItemModel`.
- **B80** **S** Eight Android view models expose `GemErrorText` and composables translate it through a `@Composable GemErrorText.text()` (`ui/localization/GemstoneText.kt:367`): add asset, support chat, wallet image, price alerts, asset details, perpetual position and setup wallet. Translate in the view model with `@ApplicationContext`, as networks and contacts do, and delete the composable overload.

## 4. App-side twins and outcomes the app invents

[No hand-written twins](ARCHITECTURE.md): a type that only crosses the FFI is used as the uniffi type, and an error is Core's error localized directly.

- **F56** **M** Android turns errors into text through two classifiers (`errorText()` for five Core types plus the raw message, `toGemErrorText()` for the confirm broadcast only), re-implements Core's `payment_error_text` in `GemPaymentException.userMessage` (so `NoPaymentOptions` shows the generic scan error where iOS shows Core's text), and wraps foreign exceptions into Core variants (`ChartViewModel.kt:61`, `SwapQuotesResult.kt:23`). Core `alien_error_text`/`payment_error_text` become `text()` methods; one Android classifier covering iOS `Errors.swift`'s set.
- **F57** **S** Android WalletConnect shows hardcoded English ("Pair to X fail", "Wallet Connect unavailable…", "Connection failed", "Authentication failed": `WalletConnectCoordinator.kt:112-293`), drops scan-pairing errors (`onError = {}`) and treats a failed disconnect as success; iOS alerts all three. Localized errors surfaced like iOS.
- **F60** **S** iOS service extensions swallow Core failures: `GemWalletSessionService.swift:8-37` returns `[]` or nil (callers return silently, `NavigationRouter.swift:173,323,342`) and invents an English "No wallet id", and `GemPerpetualDetailsService.swift:13-15` drops `setChartPeriod` errors with `try?`. Propagate or report them.
- **F61** **S** The shared abbreviated parity fixture is still missing. Android's abbreviated path uses `android.icu.text.CompactDecimalFormat`, a framework class absent from plain JVM unit tests, so a test of it throws `NullPointerException` in `:gemcore:testDebugUnitTest`; it needs Robolectric or instrumentation (X172, AUD35). iOS covers both rounding modes in [FormattedNumberTests](../ios/Packages/GemstonePrimitives/Tests/GemstonePrimitivesTests/FormattedNumberTests.swift). The rounding itself is fixed: `AbbreviatedFormatter` takes the record's rule instead of always truncating, and Android's `percentText` honours `rounding` and handles `Significant` instead of force-casting to `Fraction`. `BelowThreshold` signing was fixed with K19.
- **F62** **S** The shared row renderers print dates differently: the `AllTime` date is Today/Yesterday-or-long on iOS and `DateFormat.MEDIUM` on Android, and the `Date` row is "Today, 3:45 PM" on iOS and "Today 3:45 PM" through `DateUtils` (not `GemDayBoundaries`) on Android; iOS keeps a second `RelativeDateFormatter`. One row-date renderer per app over `GemDayBoundaries`. Typed-data timestamps in signing and simulation payloads go through the same two renderers (`SimulationPayloadFieldViewModel.swift:40`, `SimulationPayloadFieldsContent.kt:44-46`, which also prints "" for 0).

## 5. Forwarders and façades

[ARCHITECTURE.md § 7](ARCHITECTURE.md#7-at-most-one-core-service-on-ios-narrow-cases-on-android): a case that only forwards a Core call is migration debt, and one answer has one route.

- **O59** **S** "Show perpetuals" still reaches the apps through more paths than the home state: iOS [PortfolioSceneViewModel](../ios/Features/WalletTab/Sources/ViewModels/PortfolioSceneViewModel.swift) and `AssetsResultsSceneViewModel` ask their own services, and Android has three — `WalletSearchViewModel` and `AssetsResultsViewModel` call `GemAssetSelectionService.showPerpetuals`, while the home preview has a fourth path, [PerpetualsPreviewViewModel](../android/features/perpetual/viewmodels/src/main/kotlin/com/gemwallet/android/features/perpetual/viewmodels/PerpetualsPreviewViewModel.kt) reading `userConfig.showPerpetuals`. That one is a self-contained composable with its own view model, so Android's home cannot read the state field without changing the preview section's API; decide whether the section takes the flag or keeps its own. Put the search and results readers on the portfolio session, then un-export the paths that are left. `GemWalletHomeViewState.shows_perpetuals` exists now and iOS's home reads it instead of fetching alongside the view state.

## 6. Core shapes that block an app move

- **K13** **L** Fourteen Core services hold another domain's store against [ARCHITECTURE.md § 2](ARCHITECTURE.md): `GemWalletStore` in asset discovery, avatar, balance, device, subscription and wallet session; `GemAssetStore` in balance, perpetual and search; `GemAddressStore` in contact, stake, transactions and wallet; `GemPerpetualStore` in search; `GemBalanceStore`/`GemPriceAlertStore` in the stream subscription; `GemBannerStore` in wallet configuration. Per service after a cycle check: the owning service, or a narrow query service; record the ones kept and why.
- **K14** **M** `GemPerpetual` is built ad hoc with `.hypercore` at about 29 app sites, including per render in both mapper files and in a `@Composable`, to reach templates that ignore `self` (`margin_text`, `position_text`, `trigger_order_text`, `leverage_text`, `can_withdraw`), and "LONG 5x" is built at six sites. Provider-free templates move onto `GemLocalizedText` (a `Position { direction, leverage }` case) and the rows; provider answers come from the screen's service.
- **K19** **M** Two-crossing sign formatting, remaining callers: `GemAmountSign::format` ([`transactions/model.rs`](../core/gemstone/src/services/transactions/model.rs)) still prefixes a sign onto text the app formatted itself in five places — [PriceChangeViewModel](../ios/Packages/PrimitivesComponents/Sources/ViewModels/PriceChangeViewModel.swift), [AutocloseViewModel](../ios/Features/Perpetuals/Sources/ViewModels/AutocloseViewModel.swift) and [NumericViewModel](../ios/Packages/PrimitivesComponents/Sources/Types/NumericViewModel.swift) on iOS, [Simulation.kt](../android/features/confirm/viewmodels/src/main/kotlin/com/gemwallet/android/features/confirm/viewmodels/Simulation.kt) and [GetTransactionDetailsImpl](../android/data/coordinators/src/main/kotlin/com/gemwallet/android/data/coordinators/transaction/GetTransactionDetailsImpl.kt) on Android. Two of them also call Core for `sign(value:)` first, so one number costs two crossings. Route these values through `GemFormattedNumber` with `Signed` notation as the transaction row now does. Before doing so, fix the abbreviated display: `AbbreviatedFormatter` on iOS and `CompactDecimalFormat` on Android never see `showsSign`, so a signed abbreviated currency silently loses its sign. The below-threshold branch had the same blind spot and dropped the sign on iOS while emitting the wrong one on Android; both are fixed and covered by `autoValueFormatter`.
- **K21** **M** **The two stores keep the same Core data in different shapes.** `balances` holds [`BalanceMetadata`](../core/crates/primitives/src/asset_balance.rs) as one JSON `metadata` column on iOS ([BalanceRecord](../ios/Packages/Store/Sources/Models/BalanceRecord.swift)) and as five flat columns on Android (`votes`, `energy_available`, `energy_total`, `bandwidth_available`, `bandwidth_total`, [DbBalance](../android/data/services/store/src/main/kotlin/com/gemwallet/android/data/service/store/database/entities/DbBalance.kt)); `prices` carries market cap, supply and ATH/ATL in the price row on iOS and in a separate `asset_market` table on Android. Same record, two schemas, so no query or diff can be written once. Pick one shape per table and migrate the other side. While there, drop two columns nothing reads: iOS `balances.lastUsedAt` and Android `balances.list_position`. Each table is its own migration; do not bundle them. Verify a balance round-trips every metadata field on both apps.

## 7. Decisions to make

None of these is a code change until someone chooses; each is written so the choice is the only remaining step.

- **D48** **S** Opening Support turns push notifications on first on Android (`SettingsScene.kt:60-68`, permission, `enableNotifications()`, device sync) and simply opens on iOS. Decide whether support should ask; if yes, Core answers it through the permission port on both apps, otherwise delete the Android branch.
- **D72** **S** Whether an open WalletConnect request holds off the app relock differs, a security choice d974886537 left open: iOS passes `hasPendingRequest: false` (`BiometryAuthenticationService.swift:19-26`), Android passes whether a request is open (`LockTimer.kt:28-33`), and Core then skips the relock however long the app was away (`security/rules.rs:62-66`). Decide: always relock (drop the parameter and Android's input) or a bounded window both apps feed.
- **D173** **S** `just gemstone build-ios` has not compiled in a long time: its `tests/ios/Packages/Gemstone` package never declared the `BigInt` dependency the generated bindings import (now fixed), and behind it the `GemTest` sample app is stale against renamed bindings — `SwapperProvider` no longer exists in Swift and `SwapRequests.swift` resolves `SwapperOptions` to the wrong type. Decide whether the sample app still earns its place: the only check it adds over `cd ios && just build` is that the bindings compile in Swift outside the app, which the app build already proves. If it stays, repair it against the current bindings and run it in the existing checks so it cannot rot again; if not, delete `core/gemstone/tests/ios/GemTest`, the `build-ios`/`test-ios` recipes that drive it and the guide lines naming it, keeping `build-ios-lib` and `prepare-ios-package`.
- **D73** **S** WalletConnect transaction simulation failures are swallowed (`services/simulation.rs:67-80,109-111`, `unwrap_or_default`), so a failed simulation reaches the review as an empty result, indistinguishable from "this transaction changes nothing"; only the scanner's fail-open is documented. Decide the policy; if fail-open, carry a "simulation unavailable" warning through the existing warning rows.

## 8. Blocked upstream

- **X168** **S** `WalletConfiguration.multi_signature_accounts` ([`wallet_configuration.rs`](../core/crates/primitives/src/wallet_configuration.rs)) is the old name of `externally_controlled_accounts`, which also covers Solana accounts assigned to another program. The API fills both because shipped apps read only the old field. Delete the field, its fill in [`wallet_configuration.rs`](../core/apps/api/src/devices/clients/wallet_configuration.rs) and the merge in [`externally_controlled_banners`](../core/gemstone/src/services/wallet_configuration/rules.rs) on 2026-12-18, three months after the release that reads `externally_controlled_accounts`.
- **X163** **M** iOS pins the `Gemstone` package to Swift 5 language mode. Re-tested on 2026-09-16 against uniffi 0.32.1: both `uniffiTraitInterfaceCallAsync` sites still fail with "passing closure as a 'sending' parameter" because the generated `Task { }` captures three `@escaping` non-`Sendable` parameters. Nothing to decide and nothing to do until a uniffi release changes that function; re-test then. Rechecked on 2026-09-19: 0.32.1 is still the latest uniffi release, and the fix is already merged upstream as uniffi #2929 (`694fda6a05`, 2026-07-15, marks the async trait-interface closures `@Sendable`) but is not in 0.32.1. Bump uniffi to the first release that contains #2929, regenerate, and move the `Gemstone` package to Swift 6 mode.

## Coverage

The retained items came from the earlier two reviews on 2026-09-19. This architecture pass rechecked 56 distinct existing items: U14/U15/U19/U22–U25/U27–U30, K16/K18, C51, S71–S75/S79/S81–S83, R87/R94–R97/R100/R101/R104/R106/R118–R120/R122/R123/R129, D47/D49–D51/D55/D58–D61/D66/D67/D71, P89/P90, B78, F53/F55 and O59. It also narrowed the descriptions of R114/P97 and added MIG1–MIG6. U20, R87, F53, P88, S71, P89, F55 and D60 have since landed on main and are no longer open work. Source-level failure interleavings are not runtime reproductions; the tasks specify the tests still required.

**Pass one split the product by area:** transfer, confirm and swap; assets, wallet and charts; perpetuals, earn and stake; settings, rewards and WalletConnect; onboarding, NFT, fiat and activity; and the cross-cutting Core surface. Each area paired its screens across the apps and compared the service held, the session driven, and the branches, sorts, composed strings and error paths.

**Pass two ran five lenses over the whole codebase:** Core internals and the FFI surface; persistence, networking and scheduling rules; the screens pass one did not cover (scan, add token, networks, contacts, markets, lock, notifications, widget, recents, update, receive, wallet details, support); user-visible text and numbers composed in app code; and screen state machines, swallowed failures and tests that restate a Core rule.

**The completion contract above is the definition of done.** Structural censuses over fixed file sets are useful review leads, not proof of every architectural rule. Today `just check-mappers` and `just check-docs` are the checked-in checks; MIG5 adds the broader regression gate. Historical census scripts were scoped ad hoc audits. "A view never names a Core type" read zero on 2026-09-17 and had regressed by 2026-09-19 (B76–B78), so rerun the checks after every batch of row or screen migrations, not only at the end; the census recipe is in the B67 ledger entry.

### Final migration coverage check

This backlog covers the named screen families; neither its item count nor a passing structural scan proves exhaustive migration. When a family is completed, trace its normal screen, deep-link/push and child-sheet entry points through the actual Core owner and native adapters. Mark a remaining difference as an open item or a specific deliberate platform behavior, rather than declaring completion from the service field alone.

Use existing tests and reachable UI scenarios to check wallet/account/currency changes, background/resume, empty and populated storage, stale success/failure, and partial-write recovery where that flow supports them. Reuse one scenario across related entry points when it proves the same contract; do not create a test framework or an exhaustive combination matrix. Preserve exact transaction inputs and native auth gates. Add further TODOs only for a concrete missing owner, differing rule or failing contract found by that trace.

## What the sweeps taught

Read this before adding an item. Each rule below was learned by listing something that turned out not to be work, and each is stated once.

- **A sweep hit is a lead, not a verdict.** Confirm it by reading both apps before it goes on the list. Comparing the two apps first cut § 16 from 27 to 1 and § 29 from 5 to 0.
- **Count decisions, not names, files or members.** A test file named differently still covers the screen; a `var` that is a computed property is not state; a member count says nothing about ownership; a method named the same on both apps may compute different things (D15–D17). Pair a helper by what it computes.
- **The app maps; Core decides.** A `switch` over a Core enum that picks an icon, a colour, a keyboard or a localized label is the mapper contract working, not a decision. A label that *interpolates* a Core value into an app-localized sentence is where the localization belongs. A value type that only projects Core records needs no service.
- **A dash in the screen-service map means "not recorded", never "the other app decides this itself".** A coordinator or application case holding the service is the Android shape of holding it (§ 29).
- **Count a holder by the services it calls.** A reference passed straight through to the child that decides with it is a constructor argument (§ 23).
- **Retest a recorded build blocker before treating it as a boundary.** `GemTests` "cannot link", the widget "cannot call Core" and `Formatters` "cannot import Gemstone" were all one missing build setting, and each had other items closed on its authority (§ 36).
- **A number crosses as a value and a style; a locale prints it.** Currency symbols, duration text, day grouping, dust and abbreviation thresholds rendered with the device locale are platform work once Core has decided the precision, the style and the tone (§ 17, § 19, § 21).
- **Ordering that must live in a database query stays there.** Core cannot write either app's query, and sorting a thousand rows in memory per emission is the wrong trade (§ 20).
- **A default arm can be the answer.** `None`, `false`, `Err` and a named `Unknown` in a provider table are fail-closed; only an arm that picks a wrong concrete value is a gap (§ 33).
- **Test the premise, not the count.** Composition depth was said to be where cycles appear: the graph was walked, 68 services, zero cycles (§ 31). File length was said to mean an outgrown module: the production halves are a smooth distribution and the rest is `mod tests` (§ 34). Two traits with different membership were one trait and its supertrait (§ 33).
- **A migration is the one place a rule does not get consolidated.** Its behaviour has already run on installed databases (§ 24).
- **Platform APIs for one intent are not two decisions.** Room's `EXTRA_IS_SENSITIVE` against `UIPasteboard`'s expiry date, a clock-driven lock screen, an OkHttp connection pool with no `URLSession` counterpart (§ 19, § 32).
- **Exclude on every sweep:** `Gem*Store` foreign-trait implementations, Hilt `@Provides`, Room `TypeConverters`, `@Preview` composables, framework overrides, `#Preview` bodies, generated files and test kits. All are called by generated or native code no token search can see.

## Ledger of closed sections

**U35 (2026-09-19, earlier subset).** Narrowed: `GemTransferData::transaction_type` and `GemPaymentService::decode_url` stay as [the compatibility notes](ARCHITECTURE.md#intentional-platform-differences-and-compatibility) record, and `GemPaymentService::transfer_destination` is read by the iOS `PaymentDestinationBuilder`. That earlier cleanup moved its other exports to plain `impl` blocks or deleted them, together with composition-service methods only Core calls and the replaced `HyperliquidSubscriptions` object. The current U35 item tracks the remaining node helpers and fields; this ledger entry does not close those.

**K8 (2026-09-19).** Closed as correct against § 7. `GemStreamService` is the stream event router: each of its dependencies handles one event kind, so a single use each is its shape, and a composition service in front of it would add a hop per event. `GemAssetDetailsService`, `GemWalletService` and `GemPerpetualService` are screen services composing collaborators, which § 7 prescribes. Their only pure one-call forwards are `GemWalletService::address_url` to the explorer and `GemPerpetualService::add_recent` to recent activity, and § 7 names exactly that as the route by which a screen reads a composition answer. Every other held dependency does work in more than one method or builds its input.

**S35 (2026-09-19).** Decided without a session: the recents rule was the one section decision iOS still composed itself, and Core already answered it for Android (`GemSelectAssetFlow::shows_recents`), so the iOS wallet search now asks the same flow. Showing the pinned header when pinned assets or pinned perpetuals exist is layout, not a product decision: a section with no rows is not drawn, on both apps. The rest of the model is the query text and presentation state.

**S34 (2026-09-19).** Decided without a session: the rewards screen shows Core's `GemRewardsState` for the wallet Core picks (`selected_wallet`), so what the view model holds beyond that is sheets, alerts and toasts. Core `incoming_referral_code` now decides between activating and confirming an incoming code, and both apps map it.

**S37 (2026-09-19).** Decided without a session: select-asset already takes every decision from its Core `GemSelectAssetFlow` (add-token, search step, row action, deposit display, price alerts, recents), and the query, filter and selection it holds are the inputs those rules read.

**S38 (2026-09-19).** Decided: `GemConfirmation` stays a retained UniFFI operation object, not a pure session record. It loads the quote, fee and simulation and executes the transfer, and a session is a pure record the screen drives; the [service map](ARCHITECTURE.md#screen-services) describes that distinction.

**S40 (2026-09-19).** Decided without a session: `GemAmountEntry` already carries every answer the amount screen shows and is recomputed from the typed text on each change; the text belongs to the platform text field and the input type is a toggle, so a session would only rename two setters.

**X164 (2026-09-19).** Decided: no separate mobile crate boundary. The shared `primitives` types are the wire contract between the API and the apps, so splitting them would duplicate the types both sides must agree on. The rebuild cost of a `primitives` change is cut the way issue #1202 lays out: `default-members` for mobile work (A6) and moving the modules only one consumer uses out of `primitives`.

**O35 (2026-09-19).** Closed as correct: the root keeps deriving `currentWallet` from the observed `currentWalletId` in the same `body` pass. Caching it in the model breaks the one invariant that matters here, because `.onChange(of: currentWalletId)` runs after the body update: the frame after a switch would build the tabs for the previous wallet, and the frame after deleting the last wallet would build `MainTabView` for a wallet that no longer exists. Reading through the async `get_current_wallet` leaves the first frame without a wallet, so onboarding would flash at every launch, and a sync session read would put a main-thread query on Android that Room forbids. The per-pass read is one primary-key lookup, and root body passes are rare.

**D40 (2026-09-19).** Landed: the asset screen and the activity list on both apps take their transactions load state from Core (`GemAssetDetailsService::refresh` returns it beside the step failures, `GemTransactionsService::refresh` replaces the bare `sync`), and a failed refresh with nothing stored shows the error row in place of the empty state. The iOS activity list needed no new empty-state type: it draws the same error row the stake screen uses, inside the list, and keeps its empty-state overlay for the no-error case.

**V90 (2026-09-19).** Landed: R82 moved the chart market rows onto `GemListRow` (a ranked amount, an all-time row, an identifier row with copy and explorer), and the last two screens that still switched a bare row enum followed — the WalletConnect connection details (`GemConnectionDetailRow`) and the collectible info section (`GemCollectibleRow`, whose token id copies through the new `GemCopyKind::Plain`). The row enums left in Core are not plain rows: named outcomes (`GemNodeCheckRow`, `GemBalanceRow`), per-screen rich rows and composites that wrap a `GemListRow` case (confirm, swap, transaction and asset details), and layout grids (`GemSecretPhraseRow`).

**B74 (2026-09-19).** Decided per composite and written into [ARCHITECTURE § 5](ARCHITECTURE.md#a-list-row-renders-from-one-shared-row-model) by name: every rich row stays a composite on both apps (asset, recent assets, wallet, chain, NFT, transaction, delegation, validator, swap provider, network, balance), paired with its twin. The reverse split is the same weight reached two ways: iOS draws the recents and delegation rows through the row primitive with an explicit semibold title, which is the rich family's iOS form.

**R86 (2026-09-19).** Closed as correct: the decision in the simulation header, an exact amount or an unlimited approval, is already Core's `GemApprovalValue`, and both apps only turn the exact big integer into locale text with the full style. Locale formatting is the apps' half of the split, and moving it into Core would route a transaction-critical amount through `GemFormattedNumber`'s `f64`, which cannot carry a full-precision approval.

**R85 (2026-09-19).** Landed for the sign-message review, the one screen both apps build: `GemSignMessageService.preview` takes the whole `GemWalletConnectMessageRequest` and returns the app (beside a header), wallet and network rows from the request's validated chain and account, drawn by the shared renderer on both apps. Android's review scene now takes its detail rows from the caller; the one-click auth request is Android-only and keeps its wallet picker, and the connection proposal's wallet row is a picker on both apps.

**R48 (2026-09-19).** Closed as correct: none of the three is a plain row. The contact list and contact address rows carry an avatar or a chain image beside a Core-formatted address (`contact_row`, `format_address`), the price-alert rows are asset rows, and a collectible attribute's title is the name the collection defines, which a localized `GemListRowTitle` cannot carry. Every value already comes from Core, so no decision is made twice.

**O53 (2026-09-19).** Closed as correct with S45: `NameRecordViewModel.state` is the outcome of the name lookup the app runs (the task, the debounce and the cancellation stay in the app), and Core already decides every transition through `name_input_step` and `resolved_state`. `GemRecipientSession.next` takes that state as an argument, which is [what a session cannot know](ARCHITECTURE.md#a-screen-whose-state-changes-is-a-session); the import screen reads the same model.

**S48 (2026-09-19).** Closed as correct: the security toggles are optimistic mirrors of settings the platform stores securely (the keychain and biometry on iOS, `UserConfig` on Android), rolled back when the platform call fails, and every rule the screen shows already comes from Core through `GemSecurityInput` → `securitySections`. What stays app-side is platform knowledge: which biometry name to show, and that the privacy lock exists only on iOS. A Core session would hold a second copy of state the secure store owns, which is the mirror the Coverage pass already classes as app state.

**F4 (2026-09-19).** Closed as correct: `WCRequestViewModel` calls one Core service, the WalletConnect one. `GemApplicationMetadataService` is a dependency-free rule object (iOS holds the same one as `.shared`), which [the construction contract](ARCHITECTURE.md#8-services-are-injected-never-constructed-at-a-call-site) exempts, and the sign-message service is handed to the `WCRequest.SignMessage` review model the view model vends, the same split iOS makes between its request handler and `SignMessageSceneViewModel`.

What each earlier sweep closed as correct, kept so the same lead is not re-raised with the same answer. Commits carry what landed.

**K2 (2026-09-18).** Closed as correct: both apps hand the ETA seconds straight to Core's `DurationFormatter.estimate_parts` at the render site and only localize the parts, so Core already decides the rounding. `GemTransactionStateUpdate.confirmation_eta_seconds` is a stored column, not a rendered one.

**D26, C46 and C47 (2026-09-17).** The [dependency-free rule objects](ARCHITECTURE.md#8-services-are-injected-never-constructed-at-a-call-site) are held once as `.shared` on iOS, which is their contract, not a hidden global. Android's swap quote invalidation runs through `on_request_changed`; explicit `on_quote_invalidated` calls on asset select, switch and slippage change are redundant.

**Consistency review (2026-09-17).** Dismissed as platform-owned: the app-update rule (Play in-app update vs App Store release check), the keystore flows (`setup_chains` vs `migrate_to_shared_password`), CAIP-2 (both through Core by different entry points), the sign-message preview (`payload_preview` is Android's one-click-auth flow), and the auth prompt outcome read by halves (iOS needs `is_cancelled`, Android `retry_delay_milliseconds`; both are Core rules). One-sided enum mappings that pick an icon or translate enum to enum are not label choices; `just check-mappers` is the label check.

**B67 (2026-09-17).** The historical view-boundary census (a lead generator, not a blanket prohibition on shared renderer inputs): collect `pub struct|enum|trait Gem*` under `core/gemstone/src` plus the public types in `Gemstone.swift`, then list `ios/**/Scenes`, `ios/**/Views`, `*Scene.swift`, `*View.swift` and every Android file containing `@Composable` (tests, generated, the mapper files and previews excluded) that names one. It read 0 and 0 on 2026-09-17; B76–B78 are what it finds now.

**Contract checks (2026-09-16).** Kept as correct: a UI state class may carry `NameResolveIndicatorUIModel` from the shared field; the dependency-free rule objects and the Hilt providers that construct concrete services are the only concrete-class holders; view models with several `show*` members read Core sections and rules; navigation values are app types (`ConfirmTransferInput`, `ImportType`, `WalletSecretInput`); a screen flag that is a computed projection of the session phase is not a second state; a row model may carry the edit input and pre-formatted texts the record cannot; an application case with branches that only plumb a flow is a narrow read.

**U4 (2026-09-16).** `setupWalletModel` stays written twice on purpose: two parent models vending the same child is the shape the parent-vends-the-child rule asks for.

**X167 (2026-09-16).** Android view models hop to the injected `@IoDispatcher` for calls that reach the network or a database store. A `getCurrency` read stays on the calling thread: `GemPreferencesService`'s Android store is `SharedPreferences`, not Room.

**P85 (2026-09-16).** Search field sets follow iOS as the reference rather than a Core export: a list of column names crossing the FFI is the lookup wrapper the guidance bans, and neither app can run Core inside its query. Android's perpetual market search joins the server-provided `search` priority table where iOS uses `LIKE`; that structural difference stays.

**B69 (2026-09-16).** A view that builds a `@State` model from nothing but its own initializer values (`AutocloseSheet`, `QRScannerScene`, `MainTabView`) owns a value-typed model; it is not reaching into a parent's internals.

**O36 (2026-09-16).** `UserConfig` is Android's observation adapter over `GemPreferencesService`, not a forwarder: its methods write through the service and update a `StateFlow` the screens collect. iOS has the same adapter in `ObservablePreferences`. Trimming it would push `GemPreferencesService` into view models as a second Core service.

**V74 (2026-09-16).** Android's `WalletImportResult` and iOS `ImportWalletSceneResult` are navigation values, which are app types. Not a twin.

**Rows, sessions, views and ownership (§ 2–§ 5).** The add-node, contact, add-asset, import-wallet and connection-proposal screens take every decision from Core and keep only fixed titles and labels. The transactions filter needed a filter set, not a session. Types named by neither app reach an app as a nested field or payload.

**Core decides it, only one app reads it (§ 9), exports no app calls (§ 10), Core behaviour with no test (§ 11).** Most one-sided reads are the same Core answer through a different export, or a flow the other app does not have (one-click auth, invalid-word highlighting, the timed biometric retry, the developer tools). `rules.rs` functions are called by their service, not by an app.

**Platform (§ 7, § 15).** Android has no call-site `dp` literal; named constants are the component-dimension pattern. `Migrations.swift`'s `try?` are the idempotent-migration idiom. `RemoteTypeMappers.kt`, `remote_types.rs`, `chain_config.rs`, `Gemstone+Localized.swift`, the migration files and `ServicesFactory.makeServices` are long because each is one thing whose length is the number of types, chains, enums, schema steps or services. The Core `unwrap`/`expect` sites left are boot wiring or infallible by construction.

**Deferred notes (§ 7).** `from_locale_identifier`'s arms are load-bearing and tested; `FileMigrator` stays; the devices-API deprecation keeps its dated comment in the code. The TON verified-collection allowlist is that provider's signal. The currency field's height pin still does its job on iOS 26.5, measured.

**Missing tests (§ 11).** Counting tests by file name was wrong three times: `Transactions`, `ReceiveViewModel` and `ConfirmViewModel` are covered from differently named files.

**Presentation (§ 16), numbers (§ 17), invented errors (§ 18), thresholds (§ 19), collections (§ 20), time (§ 21), URLs (§ 22).** Checked and kept: labels that interpolate a Core value into an app sentence; canvas geometry; `seconds`/`minutes` helpers; the decoder-probe and directory-scan idioms; `count > 1`-style checks; HTTP timeouts and pool sizes; the pasteboard expiry; `prefix` on an app-loaded list after Core's limits; `Calendar.current` day grouping; `updatedAt` stamps; native app schemes; `openSettingsURLString`.

**Ownership (§ 23), chains (§ 24), About (§ 25), bare numbers (§ 26), service-less screens (§ 27), Core duplication (§ 28), the service map (§ 29), ports (§ 30).** Conduit holders never call their second service. `ChainImage` and the banner icon are icon maps. Bare numbers are inputs, locale-rendered values or store writes. Service-less iOS models are value types projecting Core records. Same-named per-chain functions are the convention. `ConnectionComponentMonitoring`, `WebSocketRequestProvider` and `UriHandler` carry real platform polymorphism.

**Composition depth (§ 31), sessions (§ 32), chain coverage (§ 33), long files (§ 34), the FFI surface (§ 35), boundaries (§ 36), performance (§ 37).** 68 services, zero cycles. Stateful screens that hold presentation state, one field, mirrors of stored settings, a clock, one selection projected several ways, or a form whose every rule already crosses need no session. Every defaulting arm in the signers, deployment tables and fiat mappers is fail-closed. Long Core files are short production code under long test vectors. `GemTests` links, the widget links `libgemstone.a`, and `ObservableQuery` against a Room case is the observed read in each platform's own system. No benchmark harness exists on either app; eight cold-launch samples (median 4.7 s, debug, emulator) are the baseline for one.

**Localization hygiene (§ 12).** Twenty English strings under two keys are deliberate: each names a different place in the product. `just check-mappers` compares variants its supported patterns discover in both module mapper files; it does not prove completeness.
