# Market

An asset's price chart and market data, on its asset screen.

- The asset screen shows the price chart and the asset's market data under the header.
- Chart, history and market data load at the same time; the header is usable while they arrive.

## Rules

- A price chart that cannot load shows that there is no data, and only being offline shows an error, because server text is not written for users.
